{
  "name": "clickstream-frontend",
  "version": "1.0.0",
  "description": "Frontend for clickstream pipeline",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "jsdom": "^24.0.0"
  }
}